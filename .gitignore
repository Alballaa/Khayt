# Dependencies
node_modules/

# Electron build output
build/
dist/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs
*.log
npm-debug.log*

# Old patch/archive zips
*.zip

# Unknown/temp files
ziajmqE6

# Claude session data
.claude/

# Electron user data (local runtime data, not source)
userData/

# Python cache
__pycache__/
*.pyc

# Xcode user state
ios/**/xcuserdata/
ios/**/*.xcworkspace/xcuserdata/
*.xcuserstate

# Local Workbench theme screenshot captures (scripts/shot-workbench.mjs)
.wb-shots/
build-bedready/

# Microsoft Store — generated from store/microsoft/listing.json + CHANGELOG.md.
# Both are version-stamped, so a committed copy is stale the moment the version
# bumps. Regenerate on demand: `npm run store:manual` / `npm run store:metadata`.
store/microsoft/partner-center.md
store/microsoft/metadata.json
