/* ============================================================
   Khayt — thin app entry (feature logic lives in renderer/*.js)
   ============================================================ */

/* Collections and persistence — renderer/app-state.js; UI shell — renderer/shell.js */
/* Shared helpers — renderer/app-helpers.js; logs UI state — renderer/logs.js */
/* Boot — renderer/app-boot.js (DOMContentLoaded); wire-up — renderer/wire-events.js */
