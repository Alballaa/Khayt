# Community themes

Add custom theme folders here and list them in `index.json`.

Example — after copying `_template/` to `my-shop/`:

```json
{
  "themes": [
    { "id": "my-shop", "folder": "my-shop" }
  ]
}
```

Each folder needs `manifest.json` + `tokens.css`. See `../_template/README.md`.
